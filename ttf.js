var app = require('./index'),
  {path,fs,utility} = app,
  ttfInfo = require('ttfinfo');

// var fontfile = app.Config.dir.assets+'/font/tmp.ttf'
// ttfInfo(fontfile, function(err, info) {
//      // console.log(info);
//      res.send(info);
// });
